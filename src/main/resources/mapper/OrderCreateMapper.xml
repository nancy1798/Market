<?xml version="1.0" encoding="UTF-8" ?>
    <!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="cn.asghoul.dao.OrderCreateMapper">

    <!--生成订单表 -->
    <insert id="addorder" >
        insert into order_table(orderId,
                                clientId,
                                payState,
                                money,
                                createTime,
                                commentState,
                                expressState,
                                endTime) values (
                                )
    </insert>




    </mapper>